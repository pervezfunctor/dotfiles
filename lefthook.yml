pre-commit:
  parallel: false
  commands:
    python_tools:
      files: "\\.py$"
      run: bin/python-git-hook
    shell_tools:
      files: "\\.(sh|bash|zsh|ksh)$|(^|/)[^.][^/]*$|(^|/)\\.[^./][^/]*$"
      run: bin/shell-git-hook
